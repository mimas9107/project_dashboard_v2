# 專案結構說明

```
project_dashboard_v2/
│
├── 📄 核心檔案
│   ├── mcp_server.py                      # MCP Server 主程式 ⭐
│   ├── dashboard_state.yaml               # 宣告式元數據配置檔 ⭐
│   └── requirements.txt                   # Python 依賴清單
│
├── 📚 文件
│   ├── README.md                          # 專案總覽和功能介紹
│   ├── QUICKSTART.md                      # 5分鐘快速開始指南
│   ├── DEVELOPMENT.md                     # 開發者文件（架構設計）
│   ├── CLAUDE_CODE_SETUP.md               # Claude Code CLI 設定指南 🐧
│   ├── CHANGELOG.md                       # 版本變更記錄
│   ├── PROJECT_STRUCTURE.md               # 本檔案（專案結構說明）
│   └── LICENSE                            # MIT 授權條款
│
├── 🧪 測試檔案
│   ├── test_mcp_server.py                 # 完整測試腳本（需要 mcp 套件）
│   └── test_standalone.py                 # 獨立測試腳本（不需要 mcp）✅
│
├── ⚙️ 配置範例
│   └── claude_desktop_config.example.json # Claude Desktop 配置範例
│
├── 📁 專案目錄
│   └── projects/                          # 存放所有追蹤的專案
│       ├── .gitkeep                       # 保持目錄被追蹤
│       ├── my_web_app/                    # 範例專案 1
│       ├── data_analysis_project/         # 範例專案 2
│       └── automation_scripts/            # 範例專案 3
│
└── 🔧 其他
    └── .gitignore                         # Git 忽略規則
```

## 核心檔案說明

### 1. mcp_server.py (主程式)
這是 MCP Server 的核心實作，包含：

**主要元件：**
- `Server` 實例初始化
- `list_tools()` - 列出可用工具
- `call_tool()` - 處理工具調用

**實作的工具：**
- `get_focused_projects()` - 返回 favorite 專案
- `scan_project(path, options)` - 掃描專案目錄

**輔助函數：**
- `load_dashboard_state()` - 載入 YAML 配置
- `get_git_info()` - 提取 Git 資訊
- `scan_directory()` - 遞迴目錄掃描
- `get_project_basic_info()` - 獲取基本專案資訊

**程式碼統計：**
- 行數: ~400 行
- 函數數量: 8 個主要函數
- 工具數量: 2 個 MCP 工具

### 2. dashboard_state.yaml (配置檔)
儲存宣告式元數據的 YAML 檔案。

**格式：**
```yaml
favorites:
  - "project_name_1"          # 相對路徑（相對於 projects/）
  - "project_name_2"
  - "/absolute/path/project"  # 絕對路徑
```

**特性：**
- 簡單易讀的格式
- 支援相對和絕對路徑
- 可擴展（未來可添加更多元數據）

### 3. requirements.txt (依賴清單)
Python 套件依賴：
```
mcp>=1.1.0      # Model Context Protocol SDK
pyyaml>=6.0.0   # YAML 解析器
```

## 文件說明

### README.md
- 專案總覽
- 功能特色
- 安裝指南
- 基本使用範例
- 擴展規劃

### QUICKSTART.md
- 30 秒快速安裝
- 基本配置步驟
- 實際使用範例
- 常見問題解答
- 效能參考表

### CLAUDE_CODE_SETUP.md 🐧
- Claude Code CLI 完整設定指南（Linux）
- 配置檔案位置和格式
- 多種配置範例（基本、虛擬環境、多 Server）
- 進階配置（Shell 腳本、環境變數）
- 詳細疑難排解（6+ 常見問題）
- 使用範例和最佳實踐

### DEVELOPMENT.md
- 架構設計圖
- 元數據分類說明
- 工具詳細文件
- Git 資訊提取細節
- 目錄掃描演算法
- MCP Protocol 整合
- 錯誤處理策略
- 擴展開發指南

### CHANGELOG.md
- 版本歷史記錄
- v2.0.0 變更清單
- 未來版本規劃

## 測試檔案說明

### test_mcp_server.py
完整的測試套件，測試所有輔助函數。

**需求：** 已安裝 mcp 套件

**測試內容：**
- Dashboard state 載入
- Git 資訊提取
- 目錄掃描（基本 + 過濾）
- 專案資訊獲取
- 整合工作流程模擬

### test_standalone.py ✅ 推薦
獨立測試腳本，不需要 mcp 套件。

**優點：**
- 無需安裝 mcp 即可測試
- 自動建立測試專案
- 完整的功能驗證
- 中文輸出，易於理解

**使用方式：**
```bash
python3 test_standalone.py
```

## 專案目錄結構

### projects/ 目錄
這是存放所有追蹤專案的根目錄。

**結構示例：**
```
projects/
├── .gitkeep                     # 確保空目錄被 Git 追蹤
├── my_web_app/                  # 專案 1
│   ├── .git/                    # Git 倉庫
│   ├── README.md
│   ├── app.py
│   └── ...
├── data_analysis_project/       # 專案 2
│   ├── .git/
│   ├── README.md
│   ├── analysis.ipynb
│   └── ...
└── automation_scripts/          # 專案 3
    ├── .git/
    ├── README.md
    ├── script1.py
    └── ...
```

**注意事項：**
- 可以使用絕對路徑指向外部專案
- 建議每個專案都初始化 Git 倉庫
- README.md 會被用來提取專案資訊

## 配置範例

### claude_desktop_config.example.json
Claude Desktop 的配置範例檔案。

**內容：**
```json
{
  "mcpServers": {
    "project-dashboard-v2": {
      "command": "python3",
      "args": [
        "/absolute/path/to/project_dashboard_v2/mcp_server.py"
      ],
      "env": {}
    }
  }
}
```

**使用步驟：**
1. 複製此檔案內容
2. 修改路徑為你的實際路徑
3. 貼到 Claude Desktop 配置檔案中
4. 重啟 Claude Desktop

**配置檔案位置：**
- macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`
- Windows: `%APPDATA%\Claude\claude_desktop_config.json`
- Linux: `~/.config/Claude/claude_desktop_config.json`

## 檔案大小統計

| 檔案類別 | 檔案數量 | 總大小（約） |
|---------|---------|------------|
| 核心程式碼 | 1 | ~15 KB |
| 配置檔案 | 2 | ~1 KB |
| 文件 | 6 | ~80 KB |
| 測試檔案 | 2 | ~12 KB |
| 專案範例 | 3 | ~5 KB |
| **總計** | **14+** | **~113 KB** |

## 依賴關係圖

```
mcp_server.py
    ├── depends on: mcp (Python SDK)
    ├── depends on: pyyaml
    ├── uses: subprocess (標準庫)
    ├── uses: os, pathlib (標準庫)
    └── reads: dashboard_state.yaml

dashboard_state.yaml
    ├── read by: mcp_server.py
    └── edited by: user

test_standalone.py
    ├── imports functions from: mcp_server.py (概念上)
    ├── depends on: pyyaml
    └── creates: projects/* (測試資料)
```

## 資料流程圖

```
┌──────────────┐
│  Claude LLM  │
└──────┬───────┘
       │ (1) Tool Call Request
       ↓
┌──────────────────────────────────────┐
│      mcp_server.py                   │
│  ┌────────────────────────────────┐  │
│  │ (2) Route to Tool Handler      │  │
│  └────────────┬───────────────────┘  │
│               ↓                       │
│  ┌────────────────────────────────┐  │
│  │ (3) Load Configuration         │  │
│  │     (dashboard_state.yaml)     │←─┼── dashboard_state.yaml
│  └────────────┬───────────────────┘  │
│               ↓                       │
│  ┌────────────────────────────────┐  │
│  │ (4) Scan File System           │  │
│  │     & Extract Git Info         │←─┼── projects/*
│  └────────────┬───────────────────┘  │
│               ↓                       │
│  ┌────────────────────────────────┐  │
│  │ (5) Format JSON Response       │  │
│  └────────────┬───────────────────┘  │
└───────────────┼───────────────────────┘
                ↓
       (6) Return Result
                ↓
┌──────────────────────────────────────┐
│  Claude LLM                          │
│  - Parse JSON                        │
│  - Reason about data                 │
│  - Generate user response            │
└──────────────────────────────────────┘
```

## 擴展指南

### 添加新專案
1. 在 `projects/` 目錄建立新專案資料夾
2. 初始化 Git 倉庫（建議）
3. 在 `dashboard_state.yaml` 中添加到 `favorites` 列表
4. 使用 `get_focused_projects` 工具驗證

### 添加新的元數據欄位
**宣告式元數據（在 YAML）：**
```yaml
favorites: [...]

project_tags:
  my_web_app:
    - python
    - flask
    
project_priority:
  my_web_app: high
```

**衍生式元數據（在程式碼）：**
在 `mcp_server.py` 中添加新的輔助函數並整合到工具回應中。

### 添加新工具
1. 在 `list_tools()` 中定義新工具
2. 在 `call_tool()` 中添加路由邏輯
3. 實作對應的處理函數
4. 更新文件

## 效能考量

### 瓶頸分析
1. **Git 操作** - 最耗時（已設置 5 秒超時）
2. **目錄掃描** - 次之（使用深度限制控制）
3. **YAML 解析** - 很快（通常 < 10ms）

### 優化建議
- 小型專案（< 10）: depth=3-5 無壓力
- 中型專案（10-50）: depth=2-3 推薦
- 大型專案（> 50）: depth=1-2 + 副檔名過濾

## 安全考量

✅ **已實作的安全措施：**
- 路徑驗證（檢查是否為目錄）
- 使用 `subprocess.run()` 避免命令注入
- Git 命令有超時限制
- 妥善處理 `PermissionError`

⚠️ **注意事項：**
- 不要在 YAML 中儲存敏感資訊
- 確保 `projects/` 目錄有適當權限
- 定期審查 `favorites` 列表

## 版本資訊

**當前版本：** v2.0.0
**發布日期：** 2026-01-05
**Python 要求：** >= 3.8
**MCP SDK 版本：** >= 1.1.0

## 授權

MIT License - 詳見 `LICENSE` 檔案

## 貢獻指南

歡迎貢獻！請：
1. Fork 專案
2. 建立功能分支
3. 提交 Pull Request
4. 更新文件

## 聯絡資訊

- GitHub: https://github.com/mimas9107/project_dashboard
- Issues: 歡迎在 GitHub 提出問題

---

**最後更新：** 2026-01-05
**維護者：** Project Dashboard Team
